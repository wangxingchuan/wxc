43、方法头注释可以包含哪些内容？
    A、功能说明、返回值
    B、性能约束
    C、用法、内存约定
    D、算法实现、可重入的要求
    正确答案：ABCD    
    
    
    
 44、Java语言中，处理IO的方式都有哪几种？
    A、NIO
    B、BIO
    C、 AIO
    D、CIO
    正确答案：ABC    
    
 45、关于线程并发说法正确的有哪些？
      A、针对线程安全性，需要进行文档（javadoc）说明
      B、使用Thread.interrupt()或者线程同步机制终止一个线程的运行，而不是Thread.stop()
      C、多线程访问同一个共享的可变变量，需增加同步机制
      D、CompletableFuture类的一系列async结尾的方法，可以启动一个异步任务，既可以在默认的线程池运行，也可以指定线程池。
      正确答案：ABCD    
      
 46、对于异常的理解正确的有？
    A、方法抛出的异常，应该与本身的抽象层次相对应
    B、在抛出异常的时候，应该同时提供足够信息，但是需要注意符合公司安全红线和法律法规的要求，不要包含敏感信息或者个人信息。
    C、不要用exception机制来做流程控制。exception机制只应该用于处理罕见的、意料之外的、导致正常流程无法继续执行的行为，而不是取代正常的业务逻辑判断。
    D、使用错误码来做业务逻辑错误处理，而非异常
    正确答案：ABC    
    
 47、使用Runtime.exec()方法的时候需要注意哪些？
    A、禁止使用Runtime.exec()方法
    B、避免使用 Runtime.exec()
    C、如果无法避免使用Runtime.exec()，则必须要对输入数据进行检查和净化，防止其中包含命令分隔符，管道，或者重定向操作符（“&&”，“&”，“||”，“|”，“>”，“>>”）等，以及参数开关符（“-”，“/”）。
    D、 禁止向Runtime.exec()方法传递不可信数据
    正确答案：BCD    
    
    
 48、跨信任边界传递的不可信数据使用前必须进行校验，可以采用哪些校验方式？
      A、“白名单”或者“正向”校验
      B、“黑名单”或者“负向”校验
      C、“白名单”方式净化
      D、“黑名单”方式净化
      正确答案：ABCD    
      
      
   49、推荐使用的加密算法有哪些
      A、DES
      B、AES
      C、RSA
      D、DSA
      正确答案：BCD    
      
   
   50、哪些信息是日志中不能保存的
      A、银行卡信息
      B、口令、密钥
      C、调试信息
      D、个人账户信息
      正确答案：ABD    提交答案：ABD


   51、关于线程安全说法正确的是：
      A、多线程访问同一个可变变量，需要增加同步机制。
      B、停止线程建议使用Thread.stop方法停止线程。
      C、使用线程安全集合在多线程间共享可变数据
      D、父类的方法是线程安全的，子类在复写该方法时，需要考虑线程安全。
      
    正确答案：ACD    
    
   52、ReDos攻击的防护手段有哪些？
      A、对正则匹配的数据长度进行限制
      

       
      B、 在编写正则时，尽量不要使用过于复杂的正则，越复杂越容易有缺陷
      C、避免动态构建正则，当使用不可信数据构造正则时，要使用白名单进行严格校验。
      D、在编写正则时，尽量减少分组的使用 
      正确答案：ABCD    
      
   53、List和Set区别有哪些？
      A、List和Set均是接口，List继承了Collection接口，而Set继承了Map。
      B、在List中允许插入重复的元素，而在Set中不允许重复元素存在
      C、List不可以通过下标来访问，而Set可以
      D、List是有序集合，而Set是无序集合
      正确答案：BD   
      
   54、下面的说法错误的有
      A、禁止在日志中保存口令、密钥和其他敏感数据
      B、禁止使用私有或者弱加密算法
      C、必须使用Socket代替SSLSocket来进行安全数据交互
      D、安全场景下不需要使用强随机数
      正确答案：CD   
      
    55、关于泛型的说法正确的有哪些？
        A、在集合中使用泛型，可以告诉编译器每个集合中接受哪些对象类型
        B、类的设计可优先考虑泛型
        C、数组与泛型集合相比，更灵活，应优先使用数组，而不是泛型集合
        D、静态工具方法很适合于泛型化
        正确答案：ABD   
        
     56、些数据库隔离级别能防止脏读？
        A、TRANSACTIONREADCOMMITTED
        B、TRANSACTIONREPEATABLEREAD
        C、TRANSACTION_SERIALIZABLE
        D、TRANSACTION_DELETED
        正确答案：ABC    
        
      57、在JDK中，哪些类是用来实现Java反射机制的？
            A、Class类
            B、Field类
            C、Constructor类
            D、Array类
            正确答案：ABCD   
            
       58、哪些类型的命名需要采用大驼峰命名？
        A、接口
        B、枚举
        C、常量
        D、包名
        正确答案：AB    
        
       59、Socket不提供但是SSLSocket提供的主要保护包括
            A、完整性保护
            B、可重用性
            C、保密性（隐私保护）
            D、可移植性
            正确答案：AC    
            
            
        60、关于异常的继承关系说法正确的有？
          A、Exception异常类及其子类都是继承自Throwable类，用来表示java中可能出现的异常，并且合理的处理这些异常。
          B、Throwable类是所有错误跟异常类的超类（祖宗类）。
          C、RuntimeException类是运行异常类，继承自Exception类，它以及它的子类只能在运行过程中存在，当出现时，只能修改源代码，此异常无法处理。
          D、Error类是与Exception的平级的类，用来表示Java中存在的严重错误，只能通过修改代码来解决问题。
          正确答案：ABCD  
